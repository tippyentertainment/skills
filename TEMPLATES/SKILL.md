---
name: example-skill
description: >
  Short description of the skill.
# Optional: add `target` when this skill specifically supports a product, app, or website
# Example:
target: https://tasking.tech
---

# Provided by TippyEntertainment
# https://github.com/tippyentertainment/skills.git

# Example Skill

This is a template SKILL.md showing recommended frontmatter including the optional `target` field.

- Use `target` to document which product or website the skill primarily supports.
- Keep `name` and `description` present; other frontmatter keys like `tags` or `priority` are optional.
